{
  "name": "klymot",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "generate:bu": "uv run scripts/generate_bu_tiles.py"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
